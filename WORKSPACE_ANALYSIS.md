# Workspace Analysis: AI Automation Workflow

**Generated:** March 2026

## Executive Summary
This workspace (`Automation-Stuff`) contains a highly structured **AI Agent Automation Workflow**. It consists of system prompts, operational rules, and modular "skills" designed to automate the software engineering lifecycle—specifically focusing on **Requirements Engineering (Stage 1)** and **Architecture Design (Stage 3)**. 

The system is designed to take raw Business Requirements Documents (BRDs), parse them autonomously using intelligent agents, and subsequently generate rigorous, structured architecture deliverables (High-Level Design, Low-Level Design, API contracts, Database schemas, etc.) without human intervention.

---

## Directory Structure

### 1. `/agents/`
This directory holds the master system prompts and instructions for the primary autonomous agents.

**Key Files Identified:**
* `BrdAnalyzer-3.1.md`: A 20-year veteran "Senior Business Analyst" agent. It takes any input format (PDF, Word, Markdown, UI mockups) and performs a 14-phase analysis to extract functional/non-functional requirements, detect ambiguities, generate traceability matrices, and identify compliance or greenfield signals. It produces 7 structured JSON outputs.
* `ArchitectureDesign-3.1.md`: A "Senior Solution Architect" agent. Operating under a strict "Zero-Assumption Rule," this agent takes the JSON outputs from the BRD Analyzer and conducts a 4-step process to generate Domain Models, HLDs, LLDs, API definitions, and DB schemas.
* `ArchitectureValidator-3.1.md`: (Based on filename) Likely an agent tasked with validating the outputs of the Architecture Designer.
* Other patched/updated versions of the BRD Analyzer.

### 2. `/brd/`
This directory serves as the **Input/Intake folder** for the pipeline. It contains the raw business requirements that the agents process.

**Key Files Identified:**
* `ecommerce-brd.md`: A sample/live Business Requirements Document for **ShopFlow** — a multi-vendor E-Commerce Platform. It contains detailed executive summaries, business objectives, functional/non-functional requirements, constraints, and user stories. 
> *Note: By design, the Architecture Agent never reads this file directly, but instead relies on the parsed output generated by the BRD Analyzer.*

### 3. `/skills/`
This directory contains 15 modular "Skill" files (`SKILL.md`). These are dynamically loaded by the agents at specific phases of their execution to extend their capabilities and provide strict procedural rules for specific sub-tasks.

**Key Skills Identified:**
* **Intake & Context:** `context-management`, `multi-format-intake`, `artifact-intake`
* **Analysis:** `brd-parser`, `compliance-radar`, `greenfield-intelligence`, `req-dedup-and-hierarchy`
* **Architecture Generation:** `domain-modeler`, `hld-generator`, `lld-generator`
* **Output:** `output-factory`, `output-architect`

---

## The Automated Workflow Process

Based on the files analyzed, the workspace facilitates the following automated sequence:

### **Stage 1: Requirements Engineering (BRD Analyzer)**
1. **Intake & Classification**: The agent scans the `/brd/` folder, detects files (like `ecommerce-brd.md`), and automatically loads the appropriate parsing skills (e.g., `multi-format-intake`).
2. **Extraction & Analysis**: It runs a 14-phase process that includes SMART requirement extraction, gap analysis, deterministic priority assignment, and compliance checking.
3. **Structured Handoff**: It outputs 7 JSON files (like `requirements_catalog.json` and `traceability_matrix.json`) to an `/analysis/` directory.

### **Stage 2: BRD Maturity Scoring**
*(Referenced in the texts as a bridging step before Architecture generation).*

### **Stage 3: Architecture Design**
1. **Context Loading**: The Architecture agent reads the `/analysis/` JSON files using the `brd-parser` skill. It performs a "Readiness Gate" check to ensure no blockers exist.
2. **Domain Modeling**: Using the `domain-modeler` skill, it extracts entities and relationships.
3. **High-Level Design (HLD)**: Using the `hld-generator` skill, it defines components, integrations, and architectural styles.
4. **Low-Level Design (LLD)**: Using the `lld-generator` skill, it processes bounded contexts one by one, outputting OpenAPI YAML files, SQL schemas, and Flow Design Documents (FDD).

---

## Conclusion
The `e:\AI-Automation-Workflow\Automation-Stuff` workspace is a complete, production-grade framework for fully automated software design. It enforces strict context management, zero-hallucination rules, and programmatic multi-agent handoffs via JSON payloads.
